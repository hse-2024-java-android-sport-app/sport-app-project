<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Registrationwindow"
        tools:targetApi="31">
        <activity
            android:name=".registration.RegistrationWindow"
            android:exported="true"
            android:theme="@style/Theme.Registrationwindow.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registration.TypeSelectionWindow" />
        <activity android:name=".registration.BirthDateWindow" />
        <activity android:name=".sportsman.SportsmanWindow" />
        <activity android:name=".sportsman.AddTrainingWindow" />
        <activity android:name=".sportsman.EditPlanWindow" />
    </application>
    <uses-permission android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />

</manifest>